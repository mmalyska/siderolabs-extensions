# Version tracks the PKGS tag this extension is built against (= the Talos kernel build set).
# BUILD_ARG_PKGS is passed via: make nvgpu PKGS=<tag> — e.g. v1.12.0-50-ga92bed5
VERSION: "{{ .BUILD_ARG_TAG }}"
TIER: "extra"
