name: nvgpu-toolkit
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
  # Built by mmalyska/siderolabs-pkgs feat/jetson-nvgpu branch
  # Build with: make nvgpu-toolkit PKGS=<pkgs-tag> PKGS_PREFIX=ghcr.io/mmalyska
  - image: "{{ .BUILD_ARG_PKGS_PREFIX }}/nvidia-container-toolkit-tegra-pkg:{{ .BUILD_ARG_PKGS }}"
steps:
  - install:
      - |
        # Install nvidia-container-toolkit binaries
        mkdir -p /rootfs/usr/local/bin
        cp -r /usr/local/bin/nvidia-container-* /rootfs/usr/local/bin/
        cp -r /usr/local/bin/nvidia-ctk /rootfs/usr/local/bin/ 2>/dev/null || true
      - |
        # Install l4t.csv — device node list for Tegra (from Phase 0 findings)
        mkdir -p /rootfs/etc/nvidia-container-runtime/host-files-for-container.d
        cp /pkg/files/l4t.csv /rootfs/etc/nvidia-container-runtime/host-files-for-container.d/l4t.csv
      - |
        # Install containerd runtime handler config for nvidia handler
        mkdir -p /rootfs/etc/cri/conf.d
        cp /pkg/files/20-nvidia-runtime.part /rootfs/etc/cri/conf.d/20-nvidia-runtime.part
    test:
      - |
        mkdir -p /extensions-validator-rootfs
        cp -r /rootfs/ /extensions-validator-rootfs/rootfs
        cp /pkg/manifest.yaml /extensions-validator-rootfs/manifest.yaml
        /extensions-validator validate --rootfs=/extensions-validator-rootfs --pkg-name="${PKG_NAME}"
finalize:
  - from: /rootfs
    to: /rootfs
  - from: /pkg/manifest.yaml
    to: /
