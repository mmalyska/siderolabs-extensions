name: nvgpu-toolkit
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
  # Built by mmalyska/siderolabs-pkgs feat/jetson-nvgpu branch
  # Build with: make nvgpu-toolkit PKGS=<pkgs-tag> PKGS_PREFIX=ghcr.io/mmalyska
  - image: "{{ .BUILD_ARG_PKGS_PREFIX }}/nvidia-container-toolkit-tegra-pkg:{{ .BUILD_ARG_PKGS }}"
steps:
  - install:
      - |
        # Install toolkit binaries — real binaries go to .real suffix, wrapper calls with config env
        mkdir -p /rootfs/usr/local/bin
        for bin in /usr/local/bin/nvidia-container-*; do
          cp "${bin}" "/rootfs/usr/local/bin/$(basename ${bin}).real"
        done
        cp /usr/local/bin/nvidia-ctk /rootfs/usr/local/bin/nvidia-ctk.real 2>/dev/null || true
      - |
        # Create wrapper for nvidia-container-runtime that sets XDG_CONFIG_HOME so the runtime
        # reads config from /usr/local/etc/nvidia-container-runtime/config.toml (allowed in extensions)
        # instead of the default /etc/nvidia-container-runtime/ (not allowed in extensions).
        cat > /rootfs/usr/local/bin/nvidia-container-runtime << 'WRAPPER'
        #!/bin/sh
        export XDG_CONFIG_HOME=/usr/local/etc
        exec /usr/local/bin/nvidia-container-runtime.real "$@"
        WRAPPER
        chmod 0755 /rootfs/usr/local/bin/nvidia-container-runtime
      - |
        # Install nvidia-container-runtime config pointing to the /usr/local/etc/ CSV path
        mkdir -p /rootfs/usr/local/etc/nvidia-container-runtime/host-files-for-container.d
        cp /pkg/files/l4t.csv /rootfs/usr/local/etc/nvidia-container-runtime/host-files-for-container.d/l4t.csv
        cp /pkg/files/config.toml /rootfs/usr/local/etc/nvidia-container-runtime/config.toml
      - |
        # Install containerd runtime handler config for nvidia handler
        mkdir -p /rootfs/etc/cri/conf.d
        cp /pkg/files/20-nvidia-runtime.part /rootfs/etc/cri/conf.d/20-nvidia-runtime.part
    test:
      - |
        mkdir -p /extensions-validator-rootfs
        cp -r /rootfs/ /extensions-validator-rootfs/rootfs
        cp /pkg/manifest.yaml /extensions-validator-rootfs/manifest.yaml
        /extensions-validator validate --rootfs=/extensions-validator-rootfs --pkg-name="${PKG_NAME}"
finalize:
  - from: /rootfs
    to: /rootfs
  - from: /pkg/manifest.yaml
    to: /
